spring.application.name=log-Monitoring-system
# JMX disabled to avoid RMI LocalRMIServerSocketFactory warnings.
# Enable only if remote JMX monitoring is explicitly required.
spring.jmx.enabled=false

#creating database configuration
spring.datasource.url=jdbc:postgresql://localhost:5434/logmonitor
spring.datasource.username=postgres
spring.datasource.password=mahesh@123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
